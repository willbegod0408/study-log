# study-log
나는 걔를 극복한다
# 데이터 분석가가 되기 위한 기록
  Day 1 - 2026.05.12
  정수형 말 그대로 정수 소수점없는 수 와 0 실수형 소수점이 포함된 수 하지만 1과 1.0은 다른 자료 값은 같지만  아주큰수 아주작은수는 4.24e 혹은 4.24E로 표현 
8진수(octal)는 0o 또는 0O(숫자 0 + 알파벳 소문자 o 또는 대문자 O)로 시작한다. a += 1은 a = a + 1과 동일하다. 이처럼 +=를 복합 연산자라 부른다.
파이썬은 0부터 숫자를 셈 하지만 뒤에서 부터세는 음수일때는 0부터 세지않음 문자열 슬라이싱 [0:4]이면 4번째숫자는 세지않음 3번째까지만 끝번호는 수식x 공백문자도 일반적인 문자와 동일하게 취급해서 ‘life’ 와 ‘life ‘는 다름 문자열 포매팅에서 %d %s d는 숫자 s는 문자를 사용한다 2개이상값을 넣을때는 %다음 괄호를 사용해서 컴마를 이용해서 각각의 값을 넣어 주면 된다 
%s	문자열(String)
%c	문자 1개(character)
%d	정수(Integer)
%f	부동소수(floating-point)
%o	8진수
%x	16진수
%%	Literal % (문자 % 자체)
하지만 %s는 모든값을 문자열로 바꾸어 대입 무적임
%의 경우 포매팅연산자로 쓰이고 또 안에서 문자열로 쓰일경우 문자열로 쓰일경우에는 포매팅연사자가 있고 문자열로 쓰일경우에는 반드시 %%2개로 사용한다
%10s는 전체 길이가 10개인 문자열 공간에서 대입되는 값을 오른쪽으로 정렬하고 그 앞의 나머지는 공백으로 남겨 두라는 의미다.
오른쪽 정렬은 :< 대신 :>을 사용하면 된다. 화살표의 방향을 생각하면 어느 쪽으로 정렬되는지 바로 알 수 있다.:^를 사용하면 가운데 정렬도 가능하다.
 
 
 Day 2 - 2026.05.13
 index 함수로 문자열 중 문자 t가 맨 처음으로 나온 위치를 반환했다. 만약 찾는 문자나 문자열이 존재하지 않는다면 오류가 발생한다. 앞의 find 함수와 다른 점은 문자열 안에 존재하지 않는 문자를 찾으면 오류가 발생한다는 것이다.
소문자를 대문자로 바꾸기 - upper
대문자를 소문자로 바꾸기 - lower
왼쪽 공백 지우기 - lstrip
오른쪽 공백 지우기 - rstrip
양쪽 공백 지우기 - strip
문자열 바꾸기 - replace
문자열이 알파벳으로만 구성되어 있는지 확인하기 - isalpha(공백 특수문자 숫자도 없어야됨)
문자열이 숫자로만 구성되어 있는지 확인하기 - isdigit
문자열이 특정 문자(열)로 시작하는지 확인하기 - startswith
문자열이 특정 문자(열)로 끝나는지 확인하기 - endswith
len은 문자열, 리스트 외에 앞으로 배울 튜플과 딕셔너리에도 사용할 수 있는 함수이다. 실습에서 자주 사용하므로 잘 기억해 두자. str은 정수나 실수를 문자열로 바꾸어 주는 파이썬의 내장 함수이다. 리스트에 요소 추가하기 - append
리스트 정렬 - sort
리스트 뒤집기 - reverse
인덱스 반환 - index(index(x) 함수는 리스트에 x 값이 있으면 x의 인덱스 값(위칫값)을 반환한다.)
리스트 요소 제거 - remove
리스트 요소 꺼내기 - pop
리스트에 포함된 요소 x의 개수 세기 - count
리스트 확장 - extend


 Day 3 - 2026.05.16
 
* 리스트 → [] 대괄호
* 딕셔너리 → {} 중괄호
* 튜플 → () 소괄호
딕셔너리에서  key로써는 숫자 문자열 튜플은 가능 하지만 리스트는 수정가능해서 불가능 
하지만 value로써는 아무값이나 가능 s1과 s2의 교집합을 구해 보자. &를 사용하면 교집합을 간단히 구할 수 있다
intersection 함수를 사용해도 동일한 결과를 얻는다.
이번에는 합집합을 구해 보자. |를 사용하면 합집합을 구할 수 있다. 4, 5, 6처럼 중복된 값은 1개씩만 표현된다.
s2.union(s1)을 사용해도 결과는 동일하다
마지막으로 차집합을 구해 보자. -(빼기)를 사용하면 차집합을 구할 수 있다.
difference 함수를 사용해도 차집합을 구할 수 있다.
값 여러 개 추가하기 - update
특정 값 제거하기 - remove
특정 값 제거하기 - discard
모든 값 제거하기 - clear
문자열, 리스트, 튜플, 딕셔너리 등의 값이 비어 있으면("", [], (), {}) 거짓이 되고 비어 있지 않으면 참이 된다. 숫자에서는 그 값이 0일 때 거짓이 된다. 위 표에 있는 None은 뒷부분에서 배우므로 아직은 신경 쓰지 말자. 그저 None은 값이 없음을 나타내는 객체이며, 참/거짓을 따질 때는 False처럼 동작한다는 것만 알아 두자. 
값	참 or 거짓
"python"	참
""	거짓
[1, 2, 3]	참
[]	거짓
(1, 2, 3)	참
()	거짓
{'a': 1}	참
{}	거짓
1	참
0	거짓
None	거짓
변수명 규칙
1. 영문자, 숫자, 언더스코어(_)만 사용할 수 있다.
2. 숫자로 시작할 수 없다.
3. 예약어는 사용할 수 없다.
4. 대소문자를 구분한다.
첫 번째 방법은 리스트 전체를 가리키는 [:]을 사용해서 복사하는 것이다.

리스트를 복사하는 방법으로 b = a[:], b = copy(a), b = a.copy() 세 가지를 배웠다. 셋 다 동일한 결과를 얻을 수 있지만, 별도의 모듈을 가져올 필요 없이 간편하게 사용할 수 있는 b = a[:] 또는 b = a.copy()를 주로 사용한다.
 조건문이 참이면 if 문 바로 다음 문장(if 블록)을 수행하고, 거짓이면 else 문 다음 문장(else 블록)을 수행한다. else 문은 if 문 없이 독립적으로 사용할 수 없다.
조건문 다음에 콜론(:)을 잊지 말자!

비교연산자	설명
x < y	x가 y보다 작다.
x > y	x가 y보다 크다.
x == y	x와 y가 같다.
x != y	x와 y가 같지 않다.
x >= y	x가 y보다 크거나 같다.
x <= y	x가 y보다 작거나 같다.
in	not in
x in 리스트	x not in 리스트
x in 튜플	x not in 튜플
x in 문자열	x not in 문자열
match 뒤에 비교할 변수를 쓰고, case 뒤에 해당 변수와 비교할 값을 적는다. 위에서부터 순서대로 비교하다가 일치하는 case를 만나면 그 case에 속한 문장을 수행한다. 어떤 case에도 일치하지 않으면 case _에 속한 문장을 수행한다. 여기서 _는 '그 외 모든 값'을 의미하는 와일드카드 패턴이다. if-else에서 else와 비슷한 역할을 한다고 보면 된다. "A" | "B" | "C"는 grade의 값이 이 셋 중 하나이면 해당 case를 수행하라는 뜻이다. 변수 = 참일_때_값 if 조건 else 거짓일_때_값
